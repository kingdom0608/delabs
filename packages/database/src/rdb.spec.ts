/** 데이터베이스 테이블 삭제 테스트이므로 사용전 확인 후 사용 */
describe('database', () => {
  it('rdb sync', async () => {
    // const result = await sync();
    // expect(result).toBe('sync success');
  });
});

// describe('rdb seed', () => {
//   it('rdb seed', async () => {});
// });
